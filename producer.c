int produce()
{
       while(1) 
       {
            
           /*** down(mutex2);
            * ++writecount; 
            * if(writecount == 1)
            *   down(readblock);
            * up(mutex2);
            * down(writeblock);
            * access(resource);
            * up(writeblock);
            * down(mutex2);
            * --writecount;
            * if (writecount == 0)
            *   up (readblock);
            * up(mutex2);
            * ***/
       }
}
