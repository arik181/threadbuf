#include "defs.h"
#include "buffer.h"

bufferptr initbuffer()
{
    buffer returnbuffer = (buffer) calloc(1,sizeof(buffer));
}

void writetoslot(char inputchar, slotptr writeslot)
{
//     int i = 0;
//     for(i=0;i<SIZEOFSLOT;++i)
//     {
//         if(addbuffer -> slotgroup[slot] -> content[i])
//         {
//         }
//     }
}

